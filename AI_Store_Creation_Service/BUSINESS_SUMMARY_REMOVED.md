# business_summary removal

`business_summary` has been removed from the active AI understanding contract,
state, clarification input, prompt, and public/internal response handling.
Legacy providers may still return it; the backend ignores it without failing.
