AI Store Creation Pipeline
1. Core Pipeline Steps
1. Store Description Submission
The user submits a textual description of the store idea through the system interface.This description may include general information such as the type of products, the target audience, or the overall concept of the store.
After submitting the request, the system authenticates the user and extracts the tenant_id from the authentication context to ensure proper data isolation between tenants.
The request is then forwarded to the AI Processing Service, which is responsible for generating the initial store draft.
2. Input Assessment by the AI
The system sends a generation request to the AI service containing the store description provided by the user.
Within the same request, the AI analyzes the description to determine whether the provided information is sufficient to generate an initial store draft.
The AI response may result in one of the following outcomes:
If the information is sufficient, the AI directly generates the store draft.
If the information is insufficient, the AI returns clarification questions to collect the missing information.
3. Clarification Session (MCQs)
If the AI determines that the description is not sufficient, the system presents a set of Multiple-Choice Questions (MCQs) to the user.
These questions are designed to collect the essential information required to generate the store configuration, such as:
the product category
the type of products offered
preferred store layout or template
The user selects the appropriate answers, and the responses are merged with the original store description.
The updated input is then sent again to the same AI generation process.
This clarification cycle continues until sufficient information is available for the AI to generate the store draft.
4. Store Draft Generation and Interactive Presentation
At this stage, after the initial AI-generated store draft is presented, the user has four interaction options:
Manual Edit:The user can directly modify any field (e.g., store name, description, product price).
Approve & Generate Final Store: The user confirms the draft as-is, which triggers store creation.
Partial Field Regeneration
Partial regeneration allows the user to request AI assistance to regenerate a specific field within the store draft while preserving the rest of the configuration.
Unlike manual editing, partial regeneration represents a separate AI interaction pattern that triggers a new request to the AI service.
When the user selects the regeneration option for a specific field, the system constructs a new AI request containing the following context:
the current store metadata (e.g., store name, description, category)
the full current store draft structure
the specific field that should be regenerated
constraints specifying which fields must remain unchanged
The AI service is explicitly instructed to regenerate only the selected field while maintaining compatibility with the rest of the draft.
After receiving the response, the system performs validation using the Store Draft Validation Schema to ensure that:
the regenerated field respects the defined data type and constraints
the generated value remains consistent with the store category and other related fields
If the validation succeeds, the regenerated field replaces the previous value in the store draft.If validation fails, the system rejects the generated value and allows the user to retry regeneration or continue editing manually.
Each partial regeneration request is executed within the same user session, ensuring that the AI receives the most recent version of the store draft and maintains continuity throughout the editing process.
Full Draft Regeneration
Full regeneration allows the user to request the AI to generate a completely new store draft, replacing all generated fields.
This option is useful when the user is not satisfied with the initial AI output and prefers to receive a new alternative configuration.
When full regeneration is requested, the system constructs a new AI request containing:
the original store description provided by the user
any clarification answers previously collected
the current store category or constraints defined during the session
The AI service then generates a new complete store draft, including:
store metadata
suggested products
Once the new draft is generated, the system performs mandatory validation using the Store Draft Validation Schema to verify that all required fields, data types, and structural constraints are satisfied.
If the generated draft passes validation, it replaces the previous draft and is presented to the user for further review.If validation fails, the system rejects the generated output and redirects the process to Partial Regeneration or the Fallback Flow.
Like partial regeneration, full regeneration is executed within the same user session, ensuring that the AI generation process maintains the correct tenant context and session continuity.
5. Validation (Mandatory)
1. Store Metadata Validation
Field
Type
Required
Constraints / Allowed Values
store_name
string
âœ“
Length 3â€“60 characters
store_description
string
âœ“
Length 20â€“500 characters
store_category
enum
âœ“
Must belong to a predefined list of allowed categories
store_template
string
âœ“
Must match one of the available system templates
store_language
string
âœ“
ISO code, e.g., "en", "ar", "es"
store_currency
string
âœ“
ISO currency code, e.g., "USD", "EUR", "SYP"
2. Product Draft Validation
Field
Type
Required
Constraints / Allowed Values
product_name
string
âœ“
Length 3â€“80 characters, must be unique within the store
product_description
string
âœ“
Length 10â€“300 characters
price
number
âœ“
Must be greater than 0
category
string
âœ“
Must match the store category or its subcategories
stock_quantity
integer
âœ—optional
Must be â‰¥ 0
image_url
string
âœ—optional
Must be a valid URL
3. Structural Constraints
Each store must contain 2â€“4 AI-generated products.
All products must belong to the store category or its subcategories.
Product names must be unique within the store.
All mandatory fields must be present and of the correct type before accepting the draft
4. Validation Flow
After receiving the AI-generated store draft, verify all required fields are present.
Check data types and constraints (length, allowed values, ranges).
If any validation rule fails, reject the draft and route to Partial Regeneration or Fallback Flow.
Only after passing all validation rules is the draft presented to the user for review and editing.
6. User Confirmation and Store Creation
After reviewing and optionally modifying the generated store draft, the user confirms the final store configuration.
Once the confirmation is received, the system creates the store according to the approved configuration and stores the data in the database.
All stored data is associated with the tenant_id to ensure proper tenant isolation.
7. Fallback Handling
To ensure system availability and prevent workflow interruption, the platform defines explicit fallback conditions and recovery mechanisms when the AI service fails or produces unusable responses.
6.1 Fallback Trigger Conditions
The fallback mechanism is activated when any of the following conditions occur during interaction with the AI service:
AI API Error: If the AI service returns an HTTP error response such as 500, 502, 503, or 429, the request is considered failed.
Response Timeout: If the AI service does not respond within the defined time threshold (10 seconds), the request is considered timed out and is cancelled.
Malformed or Unparseable Response: If the AI response cannot be parsed, contains invalid JSON structure, or misses critical structural elements required by the system.
Validation Failure:If the generated store draft fails to pass the mandatory validation rules defined in the Store Draft Validation Schema, such as missing required fields or violating structural constraints.
When any of these conditions occur, the system redirects the request to the fallback flow.
6.2 Fallback Flow
When fallback is triggered, the platform provides alternative options that allow the user to continue the store creation process without relying on the AI service.
These options include:
â€¢ providing a pre-built store template containing default categories and placeholder products
â€¢ allowing the user to manually configure the store structure through the interface
â€¢ enabling the user to retry the AI generation process later
These mechanisms ensure that the store creation process remains functional even when the AI service becomes temporarily unavailable.
2. AI Pipeline Design Alternatives
Before selecting the final architectural approach for the AI Store Creation Pipeline, multiple design alternatives were evaluated. Each alternative represents a different way of structuring the interaction between the system and the AI service to generate store drafts from user descriptions. The evaluation focused on balancing key concerns:آ performanceآ (response time within 3â€“5 seconds),آ usabilityآ (ability to handle incomplete input),آ complexityآ (feasibility within project scope),آ scalability,آ maintainability, andآ reliability.
The following four options were considered, ranging from the simplest single-call approach to more complex multi-step and agent-based pipelines. Each option is analyzed in terms of its advantages and disadvantages to determine the most suitable architecture for the system.
Option
Description
Advantages
Disadvantages
Option A: Single LLM Call
A single request is sent to the LLM, which directly generates the complete store output (metadata + mock products) based solely on the initial user input. No intermediate steps or interaction loops are involved.
Low latency: Only one API call â†’ supports the 3â€“5 seconds performance target.
Simple architecture: No orchestration, no state management, no multi-step coordination.
Easy to implement: Minimal backend logic required.
Poor handling of incomplete input: If the user description lacks details, the output quality degrades significantly.
No user interaction: The system cannot refine or clarify requirements dynamically.
Limited control over
output quality: All logic is delegated to a single AI response.
Option B: Multi-Step (Chained Calls)
The pipeline is broken into multiple sequential AI calls. Each step performs a specific task (e.g., extract intent â†’ generate metadata â†’ generate products), and the output of each step feeds into the next.
Better control over output: Each step can be validated and adjusted independently.
Improved structure: Clear separation of responsibilities within the pipeline.
Higher output consistency: Errors can be detected earlier in intermediate steps.
Higher latency: Multiple sequential AI calls increase total response time.
Increased complexity: Requires orchestration logic and error handling between steps.
Tighter coupling between steps: Failure in one step affects the entire pipeline.
Option C: Agentic Loop
The AI operates as an autonomous agent that iteratively plans, executes, and refines its output. It may perform multiple internal steps, retries, and decision-making loops while maintaining state across interactions.
Highly flexible: Can adapt dynamically to complex or incomplete inputs.
Advanced reasoning: Supports iterative refinement and self-correction.
Handles complex workflows: Suitable for sophisticated multi-step generation tasks.
High complexity: Requires state management, orchestration, and possibly external frameworks.
Unpredictable execution time: Difficult to guarantee performance constraints.
Over-engineering for simple use case: Adds unnecessary complexity for generating simple store drafts
Option D: Single Call + AI-Driven Clarification
A hybrid approach where the AI first evaluates the input. If insufficient, it generates clarification questions. After collecting user responses, a final AI call generates the store draft.
Balanced approach: Combines simplicity with interactive refinement.
Efficient performance: Only introduces additional steps when necessary.
Improved usability: AI guides the user instead of failing silently.
Controlled complexity: Avoids full multi-step or agentic pipelines.
Requires interaction handling: Need logic for managing Q&A flow.
Slight increase in latency: Only when clarification is needed.
3. Architectural Evaluation Criteria
To determine the most appropriate AI pipeline design, the alternatives were evaluated based on several architectural criteria derived from the system requirements and quality attributes.
The evaluation considered the following aspects:
Performance: The pipeline should maintain a response time within the target range of 3â€“5 seconds for generating the initial store draft.Usability: The system must be capable of handling incomplete or vague user input and guiding the user through clarification when necessary.Implementation Complexity: The solution should remain feasible within the scope and timeline of the project without introducing unnecessary architectural complexity.Scalability: The pipeline should support concurrent requests from multiple store owners without significant performance degradation.Maintainability: The design should allow future modifications, such as extending the generation logic or introducing additional AI features.Reliability: The system should remain functional even when AI services fail or return invalid responses.
These criteria were used to evaluate the proposed pipeline alternatives and guide the final design decision
AI Pipeline Design Alternatives & Comparison Table:
Criterion / Option
Option A: Single LLM Call
Option B: Multi-Step (Chained Calls)
Option C: Agentic Loop
Option D: Single Call + AI-Driven Clarification
Performance (â‰¤5s)
High â€“ only one call
Moderate â€“ multiple sequential calls
Low â€“ unpredictable, may exceed target
High â€“ mostly one call, extra only if clarification needed
Usability
Low â€“ no interaction
Medium â€“ some refinement possible
High â€“ adaptive, iterative guidance
High â€“ dynamic clarification improves experience
Implementation Complexity
Very Low â€“ simple
Medium â€“ requires orchestration
Low â€“ very complex, heavy state management
Low â€“ controlled complexity, simple orchestration
Scalability
High â€“ stateless, easy to scale
Medium â€“ sequential calls may slow under high load
Low â€“ stateful, complex scaling
High â€“ mostly stateless, only conditional extra calls
Maintainability
High â€“ minimal logic
Moderate â€“ multiple steps
Low â€“ complex logic, harder to maintain
High â€“ clear separation, easy extension
Reliability
Medium â€“ no clarification handling
Medium â€“ sequential error handling
Low â€“ unpredictable outputs
High â€“ validation + fallback integrated
4. Design Decision
Selected Approach:
Single Call with AI-Driven Conditional Clarification and Interactive Refinement
Justification
1. Alignment with Performance Requirements
Avoids unnecessary multiple AI calls
Maintains response time within 3â€“5 seconds
Only introduces additional interaction when required
2. Improved Usability
Users are not forced to provide all data upfront
AI dynamically guides the user through missing information
Interactive editing + partial regeneration enhances flexibility
3. Controlled Complexity
Avoids over-engineering (no agentic loops or heavy orchestration)
Keeps implementation feasible within project scope
4. Scalability Support
AI service remains stateless
Supports concurrent tenant requests via horizontal scaling
5. Maintainability
Clear separation:
AI logic
validation
business logic
Easy to extend (e.g., adding new fields or generation logic)
6. Robustness and Reliability
Validation ensures safe persistence of AI output
Fallback mechanism guarantees system availability even if AI fails
This design intentionally avoids complex AI orchestration (e.g., agentic loops) and instead adopts a controlled, interaction-driven approach that satisfies system requirements while maintaining simplicity, performance, and extensibility.
This approach was selected because the system primarily generates a relatively small amount of structured content (store metadata and a limited number of mock products). Therefore, a lightweight interaction-driven pipeline provides sufficient flexibility without introducing unnecessary architectural complexity.
